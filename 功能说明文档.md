# QStat 数据查询和统计系统 - 功能说明文档

## 📋 目录

1. [系统概述](#系统概述)
2. [技术架构](#技术架构)
3. [功能模块详解](#功能模块详解)
4. [API接口文档](#api接口文档)
5. [数据库设计](#数据库设计)
6. [使用指南](#使用指南)

---

## 系统概述

QStat 是一个基于 Spring Boot 框架的数据查询和统计系统，提供高效的数据查询、统计分析、报表导出等功能。

### 核心功能

- **多维度查询**: 支持按出生年份、飞行里程、飞行时间三种维度进行数据查询
- **灵活区间配置**: 支持自定义多个查询区间，可重叠或非重叠
- **数据可视化**: 基于 ECharts 提供柱状图、饼图、折线图等多种图表展示
- **配置持久化**: 支持保存和加载自定义查询配置
- **分页查询**: 完整的分页支持，处理大量数据

---

## 技术架构

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | Web框架 |
| MyBatis-Plus | 3.5.5 | ORM框架 |
| MySQL | 8.0+ | 关系型数据库 |
| HikariCP | - | 数据库连接池 |
| Lombok | - | 代码简化 |
| Jackson | - | JSON处理 |
| Apache POI | 5.2.5 | Excel导出 |

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Layui | 2.9.6 | UI组件库 |
| ECharts | 5.4.3 | 图表可视化 |

---

## 功能模块详解

### 模块一：用户管理模块

**位置**: `com.example.qstat.controller.UserController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 分页查询用户 | 支持按页码和每页大小查询用户列表 | `GET /user/page` |
| 按ID查询用户 | 根据用户ID获取单个用户信息 | `GET /user/{id}` |
| 新增用户 | 创建新的用户记录 | `POST /user` |
| 更新用户 | 修改用户信息 | `PUT /user` |
| 删除用户 | 删除指定用户 | `DELETE /user/{id}` |

**相关文件**:
- `UserController.java` - 控制器
- `UserService.java` - 服务接口
- `UserServiceImpl.java` - 服务实现
- `UserMapper.java` - 数据访问接口
- `User.java` - 实体类

---

### 模块二：人员数据查询模块

**位置**: `com.example.qstat.controller.PersonDataController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 分页查询人员数据 | 支持多条件组合查询和分页 | `GET /person-data/page` |
| 获取全部数据 | 获取所有人员数据列表 | `GET /person-data/list` |
| 按ID查询 | 根据人员ID获取详细信息 | `GET /person-data/{personId}` |
| 获取统计数据 | 获取人员数据的统计信息 | `GET /person-data/statistics` |
| 获取出生年份列表 | 获取所有不重复的出生年份 | `GET /person-data/birth-years` |

**相关文件**:
- `PersonDataController.java` - 控制器
- `PersonDataService.java` - 服务实现
- `PersonDataMapper.java` - 数据访问接口
- `PersonData.java` - 实体类
- `PersonDataQuery.java` - 查询条件DTO
- `PersonDataStatistics.java` - 统计结果VO

---

### 模块三：出生年份查询模块

**位置**: `com.example.qstat.controller.BirthYearQueryController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 按出生年份分页查询 | 根据年龄段和性别进行分页查询 | `POST /query/birth-year/page` |
| 按出生年份统计 | 统计各年龄段的人数分布 | `POST /query/birth-year/statistics` |

**功能特点**:
- 支持多个年龄段查询（区间可重叠）
- 支持性别筛选（全部/男/女）
- 自动计算年龄
- 提供性别分布统计和年龄段分布统计

**相关文件**:
- `BirthYearQueryController.java` - 控制器
- `BirthYearQueryService.java` - 服务接口
- `BirthYearQueryServiceImpl.java` - 服务实现
- `BirthYearQuery.java` - 查询条件DTO（包含AgeRange内部类）

---

### 模块四：飞行里程查询模块

**位置**: `com.example.qstat.controller.MileageQueryController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 按里程分页查询 | 根据里程区间和性别进行分页查询 | `POST /query/mileage/page` |
| 按里程统计 | 统计各里程区间的人数分布 | `POST /query/mileage/statistics` |

**功能特点**:
- 支持多个里程区间查询（区间可重叠）
- 支持性别筛选（全部/男/女）
- 提供性别分布统计和里程区间分布统计

**相关文件**:
- `MileageQueryController.java` - 控制器
- `MileageQueryService.java` - 服务接口
- `MileageQueryServiceImpl.java` - 服务实现
- `MileageQuery.java` - 查询条件DTO（包含MileageRange内部类）

---

### 模块五：飞行时间查询模块

**位置**: `com.example.qstat.controller.TravelTimeQueryController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 按时间分页查询 | 根据时间区间和性别进行分页查询 | `POST /query/travel-time/page` |
| 按时间统计 | 统计各时间区间的人数分布 | `POST /query/travel-time/statistics` |

**功能特点**:
- 支持多个时间区间查询（区间不允许重叠，前端会校验）
- 支持性别筛选（全部/男/女）
- 提供性别分布统计和时间区间分布统计
- 支持折线图展示时间分布趋势

**相关文件**:
- `TravelTimeQueryController.java` - 控制器
- `TravelTimeQueryService.java` - 服务接口
- `TravelTimeQueryServiceImpl.java` - 服务实现
- `TravelTimeQuery.java` - 查询条件DTO（包含TimeRange内部类）

---

### 模块六：查询模板管理模块 ⭐ 新增

**位置**: `com.example.qstat.controller.QueryTemplateController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 保存查询模板 | 保存当前的查询区间配置 | `POST /query-template` |
| 分页查询模板 | 分页获取已保存的模板列表 | `GET /query-template/page` |
| 按类型获取模板 | 根据查询类型获取模板列表 | `GET /query-template/list/{queryType}` |
| 获取模板详情 | 根据ID获取单个模板详情 | `GET /query-template/{id}` |
| 删除模板 | 删除指定的查询模板 | `DELETE /query-template/{id}` |

**功能特点**:
- 支持保存三种查询类型的配置
- 模板包含：名称、查询类型、区间配置、性别筛选、描述
- 同类型下模板名称唯一
- 支持加载模板快速填充表单

**相关文件**:
- `QueryTemplateController.java` - 控制器
- `QueryTemplateService.java` - 服务接口
- `QueryTemplateServiceImpl.java` - 服务实现
- `QueryTemplateMapper.java` - 数据访问接口
- `QueryTemplate.java` - 实体类
- `QueryTemplateQuery.java` - 查询条件DTO
- `SaveTemplateRequest.java` - 保存请求DTO
- `query_template.sql` - 数据库表脚本

**前端集成**:
- 保存按钮：`💾 保存当前配置`
- 加载按钮：`📂 加载已存配置`
- Layui 弹窗选择模板
- 自动填充表单字段

---

### 模块七：页面路由模块

**位置**: `com.example.qstat.controller.PageController`

| 功能 | 描述 | API端点 |
|------|------|---------|
| 主页路由 | 返回系统主页面 | `GET /` |
| 查询页面路由 | 返回查询页面（同主页） | `GET /query` |

**相关文件**:
- `PageController.java` - 控制器
- `index.html` - 主页面模板

---

### 模块八：异常处理模块

**位置**: `com.example.qstat.exception`

| 类 | 功能 |
|------|------|
| `GlobalExceptionHandler` | 全局异常捕获和处理 |
| `BusinessException` | 业务异常类 |

**处理的异常类型**:
- 参数校验异常
- 业务逻辑异常
- 系统异常

---

### 模块九：配置模块

**位置**: `com.example.qstat.config`

| 类 | 功能 |
|------|------|
| `MybatisPlusConfig` | MyBatis-Plus 分页插件配置 |
| `WebConfig` | Web MVC 配置、跨域配置 |
| `MyMetaObjectHandler` | 字段自动填充（创建时间、更新时间） |

---

## API接口文档

### 基础信息

- **基础路径**: `http://localhost:8088/api`
- **响应格式**: JSON

### 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... },
  "timestamp": 1723456789000
}
```

### 出生年份查询接口

#### 1. 按出生年份分页查询

```
POST /api/query/birth-year/page
Content-Type: application/json

{
  "ageRanges": [
    {"minAge": 18, "maxAge": 35},
    {"minAge": 35, "maxAge": 50}
  ],
  "gender": null,
  "current": 1,
  "size": 20
}
```

#### 2. 按出生年份统计

```
POST /api/query/birth-year/statistics
Content-Type: application/json

{
  "ageRanges": [...],
  "gender": 1
}

响应：
{
  "total": 150,
  "genderStats": [...],
  "ageRangeStats": [...]
}
```

### 里程查询接口

```
POST /api/query/mileage/page
POST /api/query/mileage/statistics
```

### 时间查询接口

```
POST /api/query/travel-time/page
POST /api/query/travel-time/statistics
```

### 模板管理接口

#### 保存模板

```
POST /api/query-template
Content-Type: application/json

{
  "templateName": "青年群体",
  "queryType": "birth-year",
  "ranges": [{"minAge": 18, "maxAge": 35}],
  "gender": null,
  "description": "18-35岁人群"
}
```

#### 获取模板列表

```
GET /api/query-template/list/birth-year
GET /api/query-template/list/mileage
GET /api/query-template/list/travel-time
```

#### 删除模板

```
DELETE /api/query-template/{id}
```

---

## 数据库设计

### person_data 表（人员数据表）

| 字段 | 类型 | 说明 |
|------|------|------|
| person_id | BIGINT | 人员ID（主键） |
| gender | TINYINT | 性别（0-女，1-男） |
| birth_year | INT | 出生年份 |
| total_mileage | BIGINT | 总里程（公里） |
| total_travel_time | BIGINT | 总时间（小时） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

### query_template 表（查询模板表）⭐ 新增

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 模板ID（主键，自增） |
| template_name | VARCHAR(100) | 模板名称 |
| query_type | VARCHAR(20) | 查询类型 |
| ranges_json | TEXT | 区间配置JSON |
| gender | TINYINT | 性别筛选 |
| description | VARCHAR(500) | 模板描述 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

**索引**:
- 主键索引: `id`
- 唯一索引: `(template_name, query_type)`
- 普通索引: `query_type`

---

## 使用指南

### 快速开始

1. **环境准备**
   ```bash
   # 确保已安装 JDK 17+
   java -version
   
   # 确保已安装 Maven 3.6+
   mvn -version
   ```

2. **数据库初始化**
   ```bash
   # 创建数据库
   mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS qsdata;"
   
   # 导入表结构
   mysql -u root -p qsdata < src/main/resources/db/schema.sql
   mysql -u root -p qsdata < src/main/resources/db/person_data.sql
   mysql -u root -p qsdata < src/main/resources/db/query_template.sql
   ```

3. **配置数据库**
   
   编辑 `src/main/resources/application.properties`:
   ```properties
   spring.datasource.url=jdbc:mysql://localhost:3306/qsdata
   spring.datasource.username=root
   spring.datasource.password=your_password
   ```

4. **运行项目**
   ```bash
   mvn spring-boot:run
   ```

5. **访问系统**
   
   打开浏览器访问: http://localhost:8088/api/

### 功能使用说明

#### 1. 出生年份查询

1. 切换到"按出生年份查询"标签
2. 点击"+ 添加年龄段"添加或删除年龄段
3. 可选：选择性别筛选
4. 点击"查询数据"执行查询
5. 查看结果列表和统计图表

#### 2. 保存查询配置

1. 配置好查询条件后
2. 点击"💾 保存当前配置"
3. 输入模板名称
4. 点击确定保存

#### 3. 加载查询配置

1. 点击"📂 加载已存配置"
2. 在弹窗中选择已保存的模板
3. 点击"应用"
4. 查询条件自动填充到表单

---

## 项目结构

```
QStat/
├── src/main/
│   ├── java/com/example/qstat/
│   │   ├── config/                    # 配置类
│   │   │   ├── MybatisPlusConfig.java
│   │   │   ├── WebConfig.java
│   │   │   └── MyMetaObjectHandler.java
│   │   ├── controller/               # 控制器层
│   │   │   ├── PageController.java
│   │   │   ├── UserController.java
│   │   │   ├── PersonDataController.java
│   │   │   ├── BirthYearQueryController.java
│   │   │   ├── MileageQueryController.java
│   │   │   ├── TravelTimeQueryController.java
│   │   │   └── QueryTemplateController.java  # ⭐ 新增
│   │   ├── service/                  # 服务层
│   │   │   ├── UserService.java
│   │   │   ├── PersonDataService.java
│   │   │   ├── BirthYearQueryService.java
│   │   │   ├── MileageQueryService.java
│   │   │   ├── TravelTimeQueryService.java
│   │   │   ├── QueryTemplateService.java     # ⭐ 新增
│   │   │   └── impl/
│   │   │       └── (各Service实现类)
│   │   ├── mapper/                   # 数据访问层
│   │   │   ├── UserMapper.java
│   │   │   ├── PersonDataMapper.java
│   │   │   └── QueryTemplateMapper.java       # ⭐ 新增
│   │   ├── entity/                   # 实体类
│   │   │   ├── BaseEntity.java
│   │   │   ├── User.java
│   │   │   ├── PersonData.java
│   │   │   └── QueryTemplate.java             # ⭐ 新增
│   │   ├── dto/                     # 数据传输对象
│   │   │   ├── PageQuery.java
│   │   │   ├── PersonDataQuery.java
│   │   │   ├── BirthYearQuery.java
│   │   │   ├── MileageQuery.java
│   │   │   ├── TravelTimeQuery.java
│   │   │   ├── QueryTemplateQuery.java       # ⭐ 新增
│   │   │   └── SaveTemplateRequest.java      # ⭐ 新增
│   │   ├── vo/                      # 视图对象
│   │   │   ├── Result.java
│   │   │   └── PersonDataStatistics.java
│   │   ├── exception/               # 异常处理
│   │   │   ├── GlobalExceptionHandler.java
│   │   │   └── BusinessException.java
│   │   └── utils/                   # 工具类
│   │       └── GeneratePPT.java
│   └── resources/
│       ├── application.properties   # 配置文件
│       ├── db/                      # 数据库脚本
│       │   ├── schema.sql
│       │   ├── person_data.sql
│       │   └── query_template.sql           # ⭐ 新增
│       └── templates/
│           └── index.html          # 主页面
└── pom.xml                         # Maven配置
```

---

## 版本历史

| 版本 | 日期 | 功能 |
|------|------|------|
| 0.0.1 | 2024-08-10 | 初始版本，基础查询功能 |
| 0.0.2 | 2024-08-11 | ⭐ 新增查询区间保存功能 |

---

## 联系方式

- **GitHub**: https://github.com/chenWilliam525/QStat
- **开发者**: QStat Team

---

*文档生成时间: 2026-08-11*
